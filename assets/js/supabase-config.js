// Fill these in with your Supabase project's own values, found under
// Project Settings > API in the Supabase dashboard. The anon/public key is
// safe to expose in client-side code -- it only grants what your Row Level
// Security policies allow (see supabase/schema.sql).
window.SUPABASE_URL = "YOUR_SUPABASE_PROJECT_URL";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";
